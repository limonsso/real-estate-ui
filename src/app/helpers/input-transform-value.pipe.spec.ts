import { InputTransformValuePipe } from './input-transform-value.pipe';

describe('InputTransformValuePipe', () => {
  it('create an instance', () => {
    const pipe = new InputTransformValuePipe();
    expect(pipe).toBeTruthy();
  });
});
